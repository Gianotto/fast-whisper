# Gere um token seguro com: openssl rand -hex 32
WHISPER_TOKEN=troque-por-token-seguro
# Modelos disponíveis: tiny, base, small, medium, large-v1, large-v2, large-v3, large, turbo
# Sufixo .en (ex: tiny.en) apenas para inglês — não usar com português
WHISPER_MODEL=base
WHISPER_LANGUAGE=pt
